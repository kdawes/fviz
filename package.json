{
  "name": "git_fviz",
  "version": "0.0.0",
  "description": "",
  "main": "bundle.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
    "build": "browserify -r ./js/Periodic:Periodic -r ./js/Runner:Runner > js/browserifybundle.js && grunt"
  },
  "repository": "",
  "author": "",
  "license": "BSD",
  "devDependencies": {
    "grunt": "~0.4.2",
    "grunt-contrib-concat": "~0.3.0",
    "grunt-contrib-uglify": "~0.2.7",
    "tape": "^2.14.0"
  }
}
